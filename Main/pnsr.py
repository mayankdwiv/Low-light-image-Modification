
import numpy as np
import cv2
# Load the reference and distorted images
# ref_image = cv2.imread('')
# dist_image = cv2.imread('')

# mse = np.mean((ref_image - dist_image) ** 2)
# if mse == 0:
#     psnr = 100
# else:
#     psnr = 20 * np.log10(255.0 / np.sqrt(mse))

# print(f'The PSNR value is: {psnr:.2f} dB')

